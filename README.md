# FreeTurn Entware Installer & Web Generator

Автоматизированный скрипт для развертывания сервера [FreeTurn](https://github.com/samosvalishe/free-turn-proxy) и удобного локального веб-генератора ссылок WireGuard на роутерах с установленной средой Entware.

## 📌 Особенности
* **Полная автоматизация:** Скрипт сам определяет архитектуру процессора (ARM, MIPS, x86) и скачивает нужный бинарный файл сервера.
* **Безопасность "из коробки":** Автоматическая генерация криптографически стойкого ключа обфускации (64 символа).
* **Умный Web-UI:** Устанавливает минималистичный веб-сервер (`lighttpd` + CGI-скрипт), который сам подтягивает ключи и порты из конфигурации роутера и генерирует готовые `freeturn://` ссылки в один клик.
* **Чистота системы:** В комплекте идет скрипт корректного удаления, который не оставляет мусора после деинсталляции.

## ⚙️ Требования
* Маршрутизатор с развернутой средой **Entware** (например, Keenetic, Netcraze, Asuswrt-Merlin, устройства на OpenWRT).
* Доступ к интернету на роутере во время установки (для загрузки пакетов и бинарника).

## 🚀 Установка (Быстрый старт)

Запустите эту команду в терминале роутера (SSH). Скрипт скачается в оперативную память и автоматически выполнит всю настройку:

```bash
wget -qO- [https://raw.githubusercontent.com/vanomilah/freeturn-entware-installer/main/install.sh](https://raw.githubusercontent.com/vanomilah/freeturn-entware-installer/main/install.sh) | sh
```
(После установки скрипт выведет в консоль прямую ссылку на панель управления локального веб-сервера).

📖 Использование
Перейдите по ссылке, которую скрипт выдал в конце установки (обычно это http://<IP-вашего-роутера>:8088/generator.cgi).

Вставьте клиентскую конфигурацию WireGuard в нижнее текстовое поле.

Нажмите «Сгенерировать ссылку».

Скопируйте готовую freeturn://... ссылку и вставьте её в приложение на телефоне или ПК.


## 🧹 Удаление
Если вы хотите полностью удалить сервер, веб-генератор и все конфигурационные файлы, выполните команду:
```bash
wget -qO- [https://raw.githubusercontent.com/vanomilah/freeturn-entware-installer/main/uninstall.sh](https://raw.githubusercontent.com/ТВОЙ_НИК/ТВОЙ_РЕПОЗИТОРИЙ/main/uninstall.sh) | sh
```
Примечание: скрипт удаления не сносит сами пакеты lighttpd, так как они могут использоваться другими сервисами на вашем роутере. При необходимости их можно удалить вручную командой opkg remove lighttpd lighttpd-mod-cgi.
